{ "version": 2, 
"rewrites": [{ "source": "/(.*)", "destination": "/routes" }],
"builds": [
    {
      "src": "./app.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "index.js"
    }
  ]
}